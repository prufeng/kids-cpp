#include<iostream>
#include <iomanip>
using namespace std;
int main() {
    float a,b,h;
    cin >> a >> b >> h;
    cout << fixed << setprecision(1)<< (a + b) * h / 2 ;
    
    

     return 0;
}
